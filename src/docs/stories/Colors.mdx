import { t } from 'i18next';
import {
  LIGHT_THEME,
  DARK_THEME,
  OCEAN_THEME,
  COMMON_COLORS,
} from '../../theme/constants/color';
import { Meta, ColorItem, ColorPalette } from '@storybook/blocks';
import { DocsTitle, DocsDescription } from '../components/TextElements';

<Meta title='Design System/Colors' />

<DocsTitle text='colors_title' />
<DocsDescription text='colors_description' />

<br />

<DocsTitle text='Light theme' />
<br />

<ColorPalette>
  {Object.entries(LIGHT_THEME).map(([key, value]) => (
    <ColorItem
      key={key}
      title={`theme.color.${key}`}
      subtitle={key}
      colors={{ 'Hex code:': value }}
    />
  ))}
</ColorPalette>

<DocsTitle text='Dark theme' />
<br />

<ColorPalette>
  {Object.entries(DARK_THEME).map(([key, value]) => (
    <ColorItem
      key={key}
      title={`theme.color.${key}`}
      subtitle={key}
      colors={{ 'Hex code:': value }}
    />
  ))}
</ColorPalette>

<DocsTitle text='Ocean theme' />
<br />

<ColorPalette>
  {Object.entries(OCEAN_THEME).map(([key, value]) => (
    <ColorItem
      key={key}
      title={`theme.color.${key}`}
      subtitle={key}
      colors={{ 'Hex code:': value }}
    />
  ))}
</ColorPalette>

<DocsTitle text='Common colors' />
<br />

<ColorPalette>
  {Object.entries(COMMON_COLORS).map(([key, value]) => (
    <ColorItem
      key={key}
      title={`theme.color.${key}`}
      subtitle={key}
      colors={{ 'Hex code:': value }}
    />
  ))}
</ColorPalette>
