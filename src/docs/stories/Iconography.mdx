import { t } from 'i18next';
import * as iconList from '../../../src/assets/icons';
import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';
import { DocsTitle, DocsDescription } from '../components/TextElements';
import { DocsIcon } from '../components/DocsIcon';

<Meta title='Design System/Icons' />

<DocsTitle text='icons_title' />
<DocsDescription text='icons_description' />

<br />

<IconGallery>
  {Object.entries(iconList).map(([name, icon]) => (
    <DocsIcon key={name} name={name} icon={icon} />
  ))}
</IconGallery>
